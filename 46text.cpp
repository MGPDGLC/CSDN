#include<iostream>
#include<cmath>
using namespace std;
int main()
{
	int a[100];
	int g;
	int n;
	cout << "ÇëÊäÈë:";
	cin >> n;
	int i, j, k;
	int x, y,z,w;
	for (g = 0; g < n; g++)
	{ 
		for (i = 0;; i++)
		{
			x = pow(3, i);
			for (j = i; j < i; j++)
				x += x;
		}

	
		cin >> n;
	cout << a[n - 1];
}